'use strict';

import chai from 'chai';
import sinon from 'sinon';

import LeaveStartCommand from './leave-start.cmd';

describe('LeaveStartCommand', () => {

	it('Should change auction when executed', () => {
		false.should.be.ok;
	});

});
